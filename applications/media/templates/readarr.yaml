apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config
  labels:
    app.kubernetes.io/name: readarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.media.storageClass }}
  resources:
    requests:
      storage: {{ .Values.readarr.configStorageSize }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: readarr
  labels:
    app.kubernetes.io/name: readarr
    app.kubernetes.io/part-of: media
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: readarr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: readarr
    spec:
      nodeSelector:
        {{- toYaml .Values.media.nodeSelector | nindent 8 }}
      containers:
        - name: readarr
          image: "{{ .Values.readarr.image.repository }}:{{ .Values.readarr.image.tag }}"
          ports:
            - containerPort: 8787
              name: http
          env:
            {{- toYaml .Values.env | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /config
            - name: media
              mountPath: /media
          resources:
            {{- toYaml .Values.readarr.resources | nindent 12 }}
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: readarr-config
        - name: media
          persistentVolumeClaim:
            claimName: media-data
---
apiVersion: v1
kind: Service
metadata:
  name: readarr
spec:
  selector:
    app.kubernetes.io/name: readarr
  ports:
    - port: 8787
      targetPort: http
      name: http
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: readarr
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`readarr.{{ .Values.domain }}`)
      kind: Rule
      services:
        - name: readarr
          port: 8787
