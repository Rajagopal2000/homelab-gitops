traefik:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      memory: 256Mi

  ports:
    web:
      exposedPort: 80
    websecure:
      exposedPort: 443

  service:
    type: LoadBalancer

  ingressRoute:
    dashboard:
      enabled: false
